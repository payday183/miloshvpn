# Codex Notes

When routine terminal access is needed for this project, prefer requesting persistent approval rules for narrow command prefixes so the user is not asked the same question repeatedly.

Useful routine prefixes:

- `git`
- `docker compose`
- `curl`

Keep approval requests narrow and explain the reason in Russian. Do not request broad rules for arbitrary interpreters or destructive commands.
