# Safe baseline that does not disable the current password login.
# Password/root login can be disabled after the owner's public key is installed and tested.
LoginGraceTime 30
MaxAuthTries 3
MaxSessions 5
X11Forwarding no
AllowAgentForwarding no
PermitEmptyPasswords no
